import argparse

import torch

import CrossEntropy
import torchfile

parser = argparse.ArgumentParser()

parser.add_argument("-i", "--path_input")
# parser.add_argument("-t", "--path_target")
# parser.add_argument("-")
